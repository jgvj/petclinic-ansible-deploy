---
# defaults file for petclinic-deploy

#root_dir: /Users/liujie
root_dir: /home/ubuntu

tomcat_dir: "{{ root_dir }}/Library/tomcat"

source_war_path: "/Users/Shared/Jenkins/Home/workspace/petclinic-pipeline/target/petclinic.war"

remote_project_dir: "{{ root_dir }}/pipeline"
